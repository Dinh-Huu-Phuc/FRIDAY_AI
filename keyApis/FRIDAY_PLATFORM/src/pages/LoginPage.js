import AuthPanel from "@/components/friday-platform/auth/AuthPanel";
import AuthTemplate from "@/templates/AuthTemplate";

export default function LoginPage() {
  return (
    <AuthTemplate>
      <AuthPanel />
    </AuthTemplate>
  );
}
